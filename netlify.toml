[build]
  base = "trackfuel-frontend"
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  NODE_ENV = "production"

[context.preview]
  command = "npm run build"
  publish = "dist"
